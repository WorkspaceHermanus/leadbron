# --- Database (Neon / Postgres) ---
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# --- PayFast ---
# Sandbox: PAYFAST_MODE=sandbox, use test credentials below
# Live: PAYFAST_MODE=live, replace with your real merchant ID/key from payfast.co.za
PAYFAST_MERCHANT_ID="10000100"
PAYFAST_MERCHANT_KEY="46f0cd694581a"
PAYFAST_PASSPHRASE=""
PAYFAST_MODE="sandbox"

# --- Resend (transactional email) ---
# Sign up free at resend.com — 3 000 emails/month on the free tier
RESEND_API_KEY="re_xxxxxxxx"
MAIL_FROM="LeadBron <onboarding@resend.dev>"

# --- App ---
# Local dev: http://localhost:3000
# Production (set this in Vercel dashboard env vars): https://leadbron.vercel.app
APP_URL="http://localhost:3000"

# --- Admin dashboard ---
# Visit /admin?token=YOUR_TOKEN — keep this secret
ADMIN_TOKEN="change-me-to-a-long-random-string"
