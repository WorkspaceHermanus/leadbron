export default function Thanks() {
  return (
    <main className="mx-auto max-w-2xl px-5 py-20 text-center">
      <h1 className="font-display text-4xl font-800 tracking-tight">Order received</h1>
      <p className="mt-4 text-moss">
        As soon as PayFast confirms your payment (usually within seconds), your
        leads are emailed to you as a CSV. If part of your order is waiting on
        stock, the balance arrives automatically as new leads come in.
      </p>
    </main>
  );
}
